/**
 * Profiles reducer
 * @author Fletcher91 <thom@argu.co>
 */

const initialState = [];

export default function profiles(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
