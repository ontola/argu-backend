
en:
  activities:
    votes:
      create: "%{owner} %{side} %{parent}"
      pro: "is in favour of"
      con: "is against"
      neutral: "is neutral about"
    decisions:
      approved: "%{owner} passed %{parent}"
      forwarded:
        to_any: "%{owner} forwarded the decision on %{parent}"
        to_group: "%{owner} forwarded the decision on %{parent} to one of your groups"
        to_you: "%{owner} forwarded the decision on %{parent} to you"
      rejected: "%{owner} rejected %{parent}"
      create: "%{owner} posted a concept decision in %{parent}"
      update: "%{owner} updated a decision on %{parent}"
    default:
      convert: "%{owner} converted %{subject} into a %{type}"
      create: "%{owner} posted a %{type} in %{parent}"
      publish: "%{owner} posted a %{type} in %{parent}"
      destroy: "%{owner} deleted %{subject}"
      trash: "%{owner} trashed %{subject}"
      untrash: "%{owner} untrashed %{subject}"
      update: "%{owner} updated %{subject}"
    arguments:
      create: "%{owner} posted a %{type} %{side} %{parent}"
      pro: "in favour of"
      con: "against"
    blog_posts:
      create: "%{owner} posted a draft %{type} in %{parent}"
    motions:
      create: "%{owner} posted a draft %{type} in %{parent}"
    questions:
      create: "%{owner} posted a draft %{type} in %{parent}"
    comments:
      create: "%{owner} replied to %{parent}"
      destroy: "%{owner} deleted a %{type} for %{parent}"
      trash: "%{owner} trashed a %{subject} for %{parent}"
      untrash: "%{owner} untrashed a %{subject} for %{parent}"
      update: "%{owner} updated a %{subject} for %{parent}"
    group_responses:
      create: "%{owner} posted a %{type} as a %{group_singular} in %{parent}"
    projects:
      create: "%{owner} posted a draft %{type} in %{parent}"
    ui:
      load_more: "Load more"
      no_more_artivities: "No more activities"
      loading: "Loading.."
    news_feed:
      title: "News feed"
