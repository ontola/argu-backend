/**
 * Phases reducer
 * @author Fletcher91 <thom@argu.co>
 */

const initialState = [];

export default function phases(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
