import RPoint from '../records/RPoint';

const initialState = [];

export default function points(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};
