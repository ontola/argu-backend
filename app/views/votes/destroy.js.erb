$('#votebtn<%= vote.parent_model.id %>')
  .fadeOut()
  .replaceWith('<%= escape_javascript(render(partial: "arguments/shr", locals: {model: vote.parent_model, vote: nil}))%>')
  .hide()
  .fadeIn();
