-content_for "activity-#{activity.id}" do
  == t('activities.arguments.create', thing: link_to(t("#{activity.trackable.motion.class_name}.type"), activity.trackable.motion, title: activity.trackable.motion.display_name))
== render_columns activity.trackable