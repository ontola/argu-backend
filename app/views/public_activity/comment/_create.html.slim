-content_for "activity-#{activity.id}" do
  == t('activities.comments.create', thing: link_to(activity.trackable.commentable.class_name.singularize, activity.trackable.commentable, title: activity.trackable.commentable.display_name))
== render_columns activity.trackable