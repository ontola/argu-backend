<div>
  <div>
    <blockquote style="padding-bottom: 20px;">
      <h1><%= t("static_pages.header") %></h1>
      <p style="text-align: center; color: grey; margin-top: -10px;"><i>John Locke</i></p>
    </blockquote>
    <div>
      <% @newstatements.first(4).each do |statement| %>
	  	  <%= render 'statements/preview', statement: statement %>
	    <% end %>
    </div>
  </div>
</div>  