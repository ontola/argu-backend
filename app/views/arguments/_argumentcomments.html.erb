<div id="commentstitle">
	<h2>Reacties</h2>
</div>
<div class="comments">
	<% @comments.each do |c| %>
		<%= render partial: 'buildcomment', locals: { c: c, depth: 1 } %>
	<% end %>
	<% if @length > 5 %>
		<%= link_to "show more", argument_path(@argument, offset: @offset + 5) unless @length < @offset + 5 %>
	<% end %>
</div>