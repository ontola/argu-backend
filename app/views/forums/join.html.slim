div[class=['.modal-container', !!defined?(no_close) ? 'no-close' : nil ].compact.join(' ')]
  .overlay
  .modal.col-1
    header
      h1= t('forums.join.header')
    .box.box-small
      section
        .form
          p= t('forums.join.body', forum: forum.display_name)
          .center
            .margin-bottom=link_to group_membership_index_path(forum.members_group, r: r||=nil), data: {method: :post, turbolinks: 'false'}, rel: :nofollow, class: 'btn btn-focus', id: 'join-forum'
              span.fa.fa-plus
              span.icon-left= t('forums.join.title')
