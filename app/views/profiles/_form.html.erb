<%= form_for(@profile) do |f| %>
  <% if @profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>
      <ul>
      <% @profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name, t("profiles.edit.name") %><br />
    <%= f.text_field :name, value: @profile.name %>
  </div>
  <div class="field">
    <%= f.label :picture, t("profiles.edit.picture") %><br />
    <%= f.text_field :picture, value: @profile.picture %>
  </div>
  <div class="field">
    <%= f.label :about, t("profiles.edit.about") %><br />
    <%= f.text_field :about, value: @profile.about %>
  </div>
  <div class="actions">
    <%= f.submit t("update_type", type: t("profiles.type")), :class => "btn btn-primary" %>
  </div>
<% end %>
