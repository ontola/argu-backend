<div id="navbar">
  <ul id="mainbar">
    <li><%= link_to "Home", '/', id: "argulogo" %></li>
    <li><%= render partial: 'search/search' %></li>
    <div id="navr">
      <% if !current_user.nil?%>
        <li><%= link_to t(:header_menuitem_settings), '/profiles/' + (current_user.username.blank? ? current_user.profile.id.to_s : current_user.username.to_s), id: "account" %>
          <ul class="dropdown">
            <li>Test</li>
          </ul></li>
      <% else %>
        <li><%= link_to t(:header_menuitem_settings), new_user_session_url, id: "account" %></li>
      <% end %>
      <li><%= link_to "Info", '/about', id: "info" %></li>
    </div>
  </ul>
</div>
<div>
  <ul class="revunderbar">
    <li><%= link_to("Stellingen", statements_url) %></li>
    <% unless current_user.nil? %>
      <li><%= link_to("Instellingen", settings_url) %></li>
      <li><%= link_to("Uitloggen", destroy_user_session_url, method: :delete) %></li>
    <% else %>
      <li><%= link_to("Registreren", new_user_registration_url) %></li>
      <li><%= link_to("Inloggen", new_user_session_url) %></li>
    <% end %>
  </ul>
</div>
<hr></hr>