.col-1
  header
    h1 _create page_

  .box
    section
      = semantic_form_for [:portal, @page] do |f|
        = f.inputs do
          = f.input :name
          = f.input :web_url

        = f.actions do
          = f.action :submit, :as => :button
          = f.action :cancel, :as => :link