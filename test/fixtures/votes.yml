# Read about fixtures at
# http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
one:
  voteable: one (Motion)
  voter: profile_two
  voter_type: 'Profile'
  for: <%= Vote.fors[:neutral] %>
#
two:
  voteable: three (Motion)
  voter: profile_two
  voter_type: 'Profile'
  for: <%= Vote.fors[:pro] %>
#
three:
  voteable: three (Motion)
  voter: profile_one
  voter_type: 'Profile'
  for: <%= Vote.fors[:con] %>
#
four:
  voteable: three (Motion)
  voter: profile_three
  voter_type: 'Profile'
  for: <%= Vote.fors[:con] %>
five:
  voteable: one (Argument)
  voter: profile_two
  voter_type: 'Profile'
  for: <%= Vote.fors[:neutral] %>
six:
  voteable: one (Question)
  voter: profile_one
  voter_type: 'Profile'
  for: <%= Vote.fors[:con] %>


page_one:
  voteable: one (Motion)
  voter: utrecht
  voter_type: 'Page'
  for: <%= Vote.fors[:neutral] %>
#
page_two:
  voteable: three (Motion)
  voter: utrecht
  voter_type: 'Page'
  for: <%= Vote.fors[:pro] %>
#
page_three:
  voteable: one (Argument)
  voter: utrecht
  voter_type: 'Page'
  for: <%= Vote.fors[:neutral] %>
